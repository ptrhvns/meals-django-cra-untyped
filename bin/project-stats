#!/usr/bin/env bash

set -Eeuo pipefail

echo
echo "### Git summary"
git summary

echo
echo "### Counting lines of source code"
echo
cloc --exclude-dir=coverage,htmlcov,node_modules,prof --exclude-ext=ini,json,md,toml .

echo
